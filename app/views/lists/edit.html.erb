<h1> Edit <%= @list.name %></h1>
<% if @list.errors.any? %>
    <% @list.errors.full_messages.each do |msg| %>
        <%= msg %>
    <% end %>
<% end %>

<%= form_for(@list) do |f| %>

<%= render partial: 'form', locals: {f: f} %>


<%= f.submit %>
<% end %>
